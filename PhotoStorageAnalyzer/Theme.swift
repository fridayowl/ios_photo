import SwiftUI

extension Color {
    // Ultra-Premium light theme color tokens
    static let mainBg = Color(red: 0.96, green: 0.96, blue: 0.98)
    static let cardBg = Color.white
    static let primaryText = Color(red: 0.08, green: 0.08, blue: 0.12)
    static let secondaryText = Color(red: 0.45, green: 0.45, blue: 0.5)
    static let borderLight = Color.black.opacity(0.06)
    static let darkCapsuleBg = Color(red: 0.90, green: 0.90, blue: 0.93)
}
